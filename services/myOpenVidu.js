'use strict';
const axios = require('axios');

module.exports = {
    async getSessionInfo(sessionId) {
        try {

        } catch (err) {
            console.error(err);
            
        }
    }
}